# 通用过滤
__pycache__/
*.py[cod]
.idea/
.vscode/
*.swp

# 大模型相关（核心过滤）
*.bin          # 大模型权重文件（如LLaMA、GPT类模型）
*.pt           # 训练 checkpoint（仅保留关键实验的小型模型）
*.ckpt
training_logs/ # 大型训练日志（提炼关键结果到文档）

# 数据相关
datasets/large_*/  # 大型数据集（仅保留小型演示数据）
*.csv.gz           # 压缩数据文件
data_cache/        # 预处理缓存

# 环境相关
venv/
env/
*.yml             # 虚拟环境配置（重要依赖写在requirements.txt）