# 基础工具（必装）
python==3.10
numpy==1.25.2
pandas==2.0.3
matplotlib==3.7.1
scikit-learn==1.2.2

# 深度学习框架（按需安装，初期可选轻量框架）
torch==2.0.1       # 主要实验框架
transformers==4.30.2  # 大模型调用与微调
tensorflow==2.13.0  # 可选，用于对比实验

# 可解释性工具（随学习进度添加）
captum==0.6.0      # PyTorch可解释性库（特征归因等）
eli5==0.13.0       # 模型解释工具

# 对抗样本工具
foolbox==3.3.1     # 对抗攻击库（FGSM、PGD等实现）
cleverhans==4.0.0  # 对抗防御实验工具

# 大模型相关（后期添加）
accelerate==0.20.3  # 分布式训练
peft==0.4.0         # 参数高效微调